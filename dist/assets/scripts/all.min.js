(function() {
  var app;

  app = angular.module('battlecruisers', []);

  app.controller('ButtonController', function() {
    this.buttons = [
      {
        'name': 'Start New Game',
        'text': 'Start New Game',
        'class': 'btn btn-start',
        'active': 'true',
        'action': function() {
          return console.log('Click!');
        }
      }
    ];
    return true;
  });

  return true;

}).call(this);
